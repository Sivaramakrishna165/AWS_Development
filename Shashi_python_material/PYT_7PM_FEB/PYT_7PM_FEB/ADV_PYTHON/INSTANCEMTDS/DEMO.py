



Instance Methods:
   * The Methods which are defined
      with in the class with self as first argument

   * Instance methods can perform the operations
   on both static and non static fields [ Instance fields]

   * Every instance method must be referred by
   self throughout the class.

   * Every instance method must be referred by an
   object reference whenever u want access it
   from outside of the class.

   
    
