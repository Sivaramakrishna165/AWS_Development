Class and object
  - self
  - instance fields
  - instance methods
  
Static Fields
  - The variables which are declared
     with in the class outside of all the methods
     called static variable

  - For static variables memory is not allocated
  with in the objects rather for these variables
  memory is allocated with in static context pool

  - For static variables only one copy of memory is
  created  and it can be  shared by no.of.object
  of same type.

  - Every static variable must be referred by class
    name or self throughout the class.

  - static variable must be referred by an object
  reference or classname whenever u want access
  if from outside of the class.





  




  
