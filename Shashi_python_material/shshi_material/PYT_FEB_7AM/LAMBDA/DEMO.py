
What is lambda ?
   - lambda is keyword , using
   lambda keyword we can define
   a name less function.

   - Using lambda function we can eliminate to define
   simplified function [ A Function which is having
                         small code | logic ]

   - We can also avoid round trips between function
   calls thus eff of the app increases.

   - lambda function implicitly returns the value | result

   - Whenever u req a function as an argument to
   another function there we can use lambda 
   
   Syn: lambda [variables] : expression 






