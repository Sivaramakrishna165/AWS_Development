Static Fields :
    - the variables which are declared
    with in the class outside of all the
    methods

    - For Static variables memory is not allocated
    with in the object  rather for these variables
    memory is allocated with in the static context pool

    - For static variables only one copy of memory is
    created and it can be shared by N no.of.Objects
    of the same type.

    - Static fields must be referred by self or classname
    throughout the class.

    - static fields must be referred by an object reference
    or classname whenever u want access it from outside
    of the class.






    
