Instance Methods
   - The methods which are defined
   with in the class with self as first
   argument.

   - instance method can perform the operations on
   both static on non static fields | instance fields

   - Instance methods must be referred by an object
   reference whenever u want access it from outside
   of the class.

   - Instance methods must be referred by self
   throughout the class.

   

   
